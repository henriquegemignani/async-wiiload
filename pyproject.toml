[build-system]
requires = [
    "setuptools>=58.0.0",
    "setuptools_scm>=3.4.3",
    "wheel>=0.37.0",
]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
minversion = "6.0"
testpaths = [
    "tests",
]
asyncio_mode = "auto"